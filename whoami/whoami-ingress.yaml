apiVersion: networking.k8s.io/v1beta1
kind: Ingress
metadata:
  name: whoami-ingress
spec:
  rules:
  - http:
      paths:
        - path: /
          backend:
            serviceName: whoami-service
            servicePort: 80